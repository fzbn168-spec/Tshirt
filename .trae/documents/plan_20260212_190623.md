我已对项目进行了彻底的审查。确实如您所言，项目中存在大量历史遗留的脚本和配置文件，这些“噪音”严重干扰了维护工作，并可能导致配置冲突。

我的清理与修复计划如下：

## 1. 彻底清理 Nginx 配置 (单一事实来源)
*   **现状确认**: `d:\网站\nginx\conf.d\` 下目前**只剩下** `prod.conf`。
*   **消除隐患**: `app.conf.template` 和 `default.conf` 在本地已不存在（如果存在我会再次强制删除）。
*   **唯一标准**: 确认 `docker-compose.prod.yml` 显式挂载了 `prod.conf` 到容器的 `/etc/nginx/conf.d/default.conf`。这意味着服务器将**严格按照** `prod.conf` 运行，不会受到任何默认配置或模板生成的干扰。

## 2. 根目录“大扫除” (删除死代码/冗余脚本)
根目录下有 17 个脚本文件是不同开发阶段的产物，现在它们大部分功能重叠或已过时。我将**删除**以下文件，只保留核心配置：
*   **删除部署类旧脚本**: `deploy.sh`, `deploy-and-test.sh`, `fix-deploy.sh`, `force-deploy.sh`, `init-prod.sh`, `setup-server.sh` (现在统一使用标准的 `docker compose` 命令)
*   **删除环境重置类脚本**: `reset-backend.sh`, `reset-frontend.sh` (现在使用 `docker compose down -v`)
*   **删除功能性旧脚本**: `setup-ssl.sh` (已由 `scripts/init-letsencrypt.sh` 取代), `update-db.sh`, `check-health.sh`, `diagnose_v2.sh`, `apply-i18n.sh` 等。

## 3. 标准化操作流程 (SOP)
清理后，项目将变得非常清爽。未来的操作将只有以下三步：
1.  **修改**: 在本地修改代码或 `prod.conf`。
2.  **同步**: `git add .` -> `git commit` -> `git push`。
3.  **部署**: 在服务器上 `git pull` -> `docker compose -f docker-compose.prod.yml up -d --build`。

执行此计划将彻底解决“文件打架”的问题，还项目一个清晰的结构。请确认是否执行清理。