我将为您升级 `OrdersService` 中的单证生成逻辑，使其基于真实的数据库字段生成专业的 Commercial Invoice (CI) 和 Packing List (PL)。

### 1. 修改 `OrdersService` 逻辑
*   **更新 `findOne` 方法**
    *   扩展 `include` 查询，确保 `items` 关联中包含 `product` (获取 HS Code, Material) 和 `sku` (获取净重、毛重、装箱数) 信息。
*   **升级 `generateCi` (商业发票)**
    *   在表头增加 **Incoterms** (贸易条款)、**Port of Loading** (起运港)、**Port of Destination** (目的港) 的展示。
    *   在商品表格中新增 **HS Code** 列。
    *   增加 **Country of Origin: China** 声明。
*   **升级 `generatePl` (装箱单)**
    *   **箱数计算 (Cartons)**: 使用 `Math.ceil(quantity / sku.itemsPerCarton)` 计算，若数据缺失则默认 1 箱。
    *   **重量计算 (Weight)**:
        *   Net Weight (N.W) = `quantity * sku.netWeight`
        *   Gross Weight (G.W) = `quantity * sku.grossWeight` (假设毛重为单件毛重) 或基于箱重计算。
    *   **体积计算 (Measurement)**:
        *   Total Volume (CBM) = `cartons * (length * width * height / 1,000,000)`。
    *   在 PDF 中增加 **Shipping Marks** (唛头) 展示区域。
    *   在底部增加 **Total Cartons**, **Total N.W**, **Total G.W**, **Total CBM** 的汇总行。

### 2. 代码实现细节
我将直接编辑 `backend/src/orders/orders.service.ts` 文件，替换现有的 Mock 逻辑。

### 3. 验证计划
由于无法直接查看生成的 PDF，我将确保代码逻辑正确处理 `null` 值（例如当 SKU 缺少重量数据时提供合理的默认值或空值），并确保数学计算逻辑无误。
